# Core dependencies
torch>=2.0.0
torchvision
gradio>=6.0.0,<7.0.0
Pillow
numpy<2.0.0

# ComfyUI dependencies (if not using existing ComfyUI installation)
# These are needed if you're running ComfyUI nodes directly
safetensors
transformers
accelerate
git+https://github.com/huggingface/diffusers.git
huggingface-hub>=0.34.0,<1.0
omegaconf
einops
tqdm
psutil
kornia
spandrel
soundfile

# Optional for better performance
xformers; platform_system != "Darwin"
