# Core dependencies
gradio==5.50.0
fastapi>=0.115.5
starlette>=0.40.0
Pillow
numpy<2.0.0
opencv-python-headless>=4.8.0,<4.9.0

# Diffusers and HuggingFace
diffusers==0.38.0
# Last release before v1. Keeps transformers on 4.x, which is the branch
# Z-Image-Turbo's Qwen3 text encoder was authored against.
huggingface_hub==0.36.2
hf_xet
transformers
safetensors
accelerate

# Additional dependencies
omegaconf
einops
tqdm
psutil
kornia
spandrel
soundfile

# NOTE: torch/torchvision/torchaudio/xformers are installed by torch.js,
# which pins them to the correct CUDA/ROCm build for the host GPU.
